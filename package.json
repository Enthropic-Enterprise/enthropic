{
  "name": "enthropic",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev:auth\" \"npm run dev:risk\" \"npm run dev:nats-gateway\" \"npm run dev:dashboard\"",
    "build": "npm run build:auth && npm run build:risk && npm run build:nats-gateway && npm run build:dashboard",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
