apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "enthropic.fullname" . }}-secrets
  labels:
    app: {{ include "enthropic.fullname" . }}
spec:
  refreshInterval: {{ .Values.externalSecret.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecret.secretStore }}
    kind: SecretStore
  target:
    name: {{ include "enthropic.fullname" . }}-secrets
    creationPolicy: Owner
  data:
    # Database
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: enthropic/{{ .Values.environment }}
        property: postgres-password

    # Redis
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: enthropic/{{ .Values.environment }}
        property: redis-password

    # JWT
    - secretKey: JWT_SECRET
      remoteRef:
        key: enthropic/{{ .Values.environment }}
        property: jwt-secret

    # NATS
    - secretKey: NATS_PASSWORD
      remoteRef:
        key: enthropic/{{ .Values.environment }}
        property: nats-password

    # Grafana (opsional)
    - secretKey: GF_SECURITY_ADMIN_PASSWORD
      remoteRef:
        key: enthropic/{{ .Values.environment }}
        property: grafana-password
